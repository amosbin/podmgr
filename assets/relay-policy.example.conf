# Relay policy example (C reconciler format)
# default deny until hub_enabled=true
hub_enabled=true

# tenant <name> allow_enroll=<bool> allowed_modes=<csv> allowed_groups=<csv>
tenant traefik allow_enroll=true allowed_modes=discoverer allowed_groups=abc,ops
tenant app1 allow_enroll=true allowed_modes=discoverable allowed_groups=abc
