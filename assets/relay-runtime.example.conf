# Runtime authorization snapshot for relay hub daemon.
# This file is the input to podmgr-relay-hubd.

tenant traefik mode=discoverer groups=abc
tenant app1 mode=discoverable groups=abc

route source=traefik target=app1 host=app1.localhost path_prefix=/ backend=unix:///tmp/podmgr-relay-app1.sock
